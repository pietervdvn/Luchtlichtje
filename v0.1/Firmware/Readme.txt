The firmware for version 1.0 has been merged with the firmware for 2.0.

The major difference between both is the addition of brightness-sensor. For a v1.0 board, disable it in the firmware by setting the USE_BRIGHTNESS -macro to false
